// // so the typescript compiler doesn't complain about the global config object
// // @ts-ignore
// declare var config = {apiUrl= 'localhost:3000'};
declare var stripe: any;
declare var elements: any;
